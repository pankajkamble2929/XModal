# XModal – React Modal Form

## 📌 Overview

XModal is a React-based modal form that opens upon clicking **Open Form**.  
The form validates **username, email, phone number, and date of birth**, and closes on clicking outside the modal.

---

## 🚀 Features

- Modal opens via "Open Form" button
- Form validation:
  - All fields required
  - Email must contain "@"
  - Phone must be 10 digits
  - DOB must not be a future date
- Clicking outside the modal closes it
- Fully passes provided Cypress tests

---

## 🛠 Installation

- npm install
- npm start
